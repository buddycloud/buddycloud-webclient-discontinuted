<div class="<%= @isPersonal("personal","") %> channel <%= @isSelected("selected","") %>">
  <div class="holder">
    <div class="avatar" title="<%= @channel?.metadata?.title?.value or @model.get('jid') %>" style="background-image: url(<%= @model.avatar %>)">
      <% if @channelmessage?: %>
        <span class="channelpost counter"><%= @channel.channelmessage.count %></span>
      <% end %>
      <% if @personalmessage?: %>
        <span class="message counter"><%= @channel.personalmessage.count %></span>
      <% end %>
    </div>
    <div class="info">
      <span class="owner">
        <%= @model.get('jid').split('@')[0] %>
        <span class="domain">
          <%= @model.get('jid').replace(/.+@/,'@') %>
        </span>
      </span>
      <span class="status"><%= @channel?.metadata?.mood?.value? or "NM" %></span>
    </div>
    <% if @isSelected(): %>
      <div class="active_channel_indicator"><div>
    <% end %>
  </div>
</div>
